import React from 'react';
import ReactLoading from 'react-loading';

const Loading = ({ type, color }) => (
	
	<ReactLoading type={"balls"} color={"#ADD8E6"} height={667} width={375} className="loading-gif" />
);

export default Loading;